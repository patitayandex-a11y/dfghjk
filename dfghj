-- NEXUS HUB
-- ESP + Aimbot + Hitbox Expander
-- Открытие: Insert | Паника: RightShift

local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")

local Menu = {
    Systems = {},
    Containers = {},
    Tabs = {},
    Defaults = {
        AimbotFOV = 60,
        AimbotSmooth = 35,
        AimbotKey = Enum.KeyCode.E,
        HitboxSize = 2.5,
        ESPColor = Color3.fromRGB(255, 0, 0),
    },
}

-- ============ АНТИ-ОБНАРУЖЕНИЕ ============
local AntiDetect = {}

function AntiDetect:ProtectGui(gui)
    pcall(function()
        if syn and syn.protect_gui then
            syn.protect_gui(gui)
        end
        if gethui then
            gui.Parent = gethui()
        end
    end)
end

function AntiDetect:RandomizeGuiName(gui)
    local chars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
    local name = ""
    for i = 1, 15 do
        name = name .. chars:sub(math.random(1, #chars), math.random(1, #chars))
    end
    gui.Name = name
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NexusMenu"
ScreenGui.Parent = Player:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
ScreenGui.DisplayOrder = 999999

AntiDetect:ProtectGui(ScreenGui)
AntiDetect:RandomizeGuiName(ScreenGui)

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 400, 0, 500)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -250)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
MainFrame.BorderSizePixel = 0
MainFrame.ZIndex = 999999
MainFrame.Visible = false
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 15)
UICorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(120, 140, 255)
MainStroke.Transparency = 0.6
MainStroke.Thickness = 1.5
MainStroke.Parent = MainFrame

-- ============ HEADER ============
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 45)
Header.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
Header.BorderSizePixel = 0
Header.ZIndex = 1000000
Header.Parent = MainFrame

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 15)
HeaderCorner.Parent = Header

local HeaderFill = Instance.new("Frame")
HeaderFill.Size = UDim2.new(1, 0, 0, 22)
HeaderFill.Position = UDim2.new(0, 0, 0, 23)
HeaderFill.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
HeaderFill.BorderSizePixel = 0
HeaderFill.ZIndex = 1000000
HeaderFill.Parent = Header

local Logo = Instance.new("TextLabel")
Logo.Size = UDim2.new(0, 28, 0, 28)
Logo.Position = UDim2.new(0, 12, 0.5, -14)
Logo.BackgroundColor3 = Color3.fromRGB(120, 140, 255)
Logo.TextColor3 = Color3.fromRGB(255, 255, 255)
Logo.Text = "✦"
Logo.Font = Enum.Font.GothamBold
Logo.TextSize = 16
Logo.ZIndex = 1000002
Logo.Parent = Header

local LogoCorner = Instance.new("UICorner")
LogoCorner.CornerRadius = UDim.new(0, 8)
LogoCorner.Parent = Logo

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -200, 1, 0)
Title.Position = UDim2.new(0, 50, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "NEXUS"
Title.TextColor3 = Color3.fromRGB(230, 230, 240)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 17
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 1000001
Title.Parent = Header

local KillButton = Instance.new("TextButton")
KillButton.Size = UDim2.new(0, 60, 0, 30)
KillButton.Position = UDim2.new(1, -120, 0, 8)
KillButton.BackgroundColor3 = Color3.fromRGB(120, 40, 40)
KillButton.BorderSizePixel = 0
KillButton.Text = "KILL"
KillButton.TextColor3 = Color3.fromRGB(220, 220, 220)
KillButton.Font = Enum.Font.GothamBold
KillButton.TextSize = 13
KillButton.AutoButtonColor = false
KillButton.ZIndex = 1000002
KillButton.Parent = Header

local KillCorner = Instance.new("UICorner")
KillCorner.CornerRadius = UDim.new(0, 15)
KillCorner.Parent = KillButton

local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 35, 0, 30)
CloseButton.Position = UDim2.new(1, -50, 0, 8)
CloseButton.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
CloseButton.BorderSizePixel = 0
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(200, 200, 200)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 16
CloseButton.AutoButtonColor = false
CloseButton.ZIndex = 1000002
CloseButton.Parent = Header

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 15)
CloseCorner.Parent = CloseButton

local TabPanel = Instance.new("Frame")
TabPanel.Size = UDim2.new(1, 0, 0, 45)
TabPanel.Position = UDim2.new(0, 0, 0, 45)
TabPanel.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
TabPanel.BorderSizePixel = 0
TabPanel.ZIndex = 1000000
TabPanel.Parent = MainFrame

local function Rounded(parent, radius)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius or 10)
    corner.Parent = parent
    return corner
end

-- ============ WATERMARK + FPS/PING ============
local Watermark = Instance.new("Frame")
Watermark.Size = UDim2.new(0, 260, 0, 26)
Watermark.Position = UDim2.new(0, 15, 0, 15)
Watermark.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
Watermark.BackgroundTransparency = 0.2
Watermark.BorderSizePixel = 0
Watermark.ZIndex = 999990
Watermark.Parent = ScreenGui

Rounded(Watermark, 8)

local WmStroke = Instance.new("UIStroke")
WmStroke.Color = Color3.fromRGB(120, 140, 255)
WmStroke.Transparency = 0.5
WmStroke.Thickness = 1
WmStroke.Parent = Watermark

local WmText = Instance.new("TextLabel")
WmText.Size = UDim2.new(1, -12, 1, 0)
WmText.Position = UDim2.new(0, 6, 0, 0)
WmText.BackgroundTransparency = 1
WmText.Text = "NEXUS | by " .. Player.Name
WmText.TextColor3 = Color3.fromRGB(220, 220, 230)
WmText.Font = Enum.Font.Gotham
WmText.TextSize = 11
WmText.TextXAlignment = Enum.TextXAlignment.Left
WmText.ZIndex = 999991
WmText.Parent = Watermark

-- FPS / Ping счётчик
local Stats = Instance.new("TextLabel")
Stats.Size = UDim2.new(0, 90, 1, 0)
Stats.Position = UDim2.new(1, -95, 0, 0)
Stats.BackgroundTransparency = 1
Stats.Text = "FPS: 0 | 0ms"
Stats.TextColor3 = Color3.fromRGB(120, 200, 255)
Stats.Font = Enum.Font.GothamBold
Stats.TextSize = 11
Stats.TextXAlignment = Enum.TextXAlignment.Right
Stats.ZIndex = 999991
Stats.Parent = Watermark

-- Подсчёт FPS
local fpsCount = 0
local fpsTime = tick()
local currentFPS = 0

RunService.RenderStepped:Connect(function()
    fpsCount = fpsCount + 1
    if tick() - fpsTime >= 1 then
        currentFPS = fpsCount
        fpsCount = 0
        fpsTime = tick()
    end
end)

-- Обновление Stats раз в 0.5 сек
task.spawn(function()
    while Watermark and Watermark.Parent do
        local ping = 0
        pcall(function()
            ping = math.floor(Player:GetNetworkPing() * 1000)
        end)
        Stats.Text = "FPS: " .. currentFPS .. " | " .. ping .. "ms"
        task.wait(0.5)
    end
end)

-- ============ MENU METHODS ============
function Menu:CreateTab(name, icon)
    local container = Instance.new("ScrollingFrame")
    container.Size = UDim2.new(1, -30, 1, -105)
    container.Position = UDim2.new(0, 15, 0, 95)
    container.BackgroundTransparency = 1
    container.BorderSizePixel = 0
    container.ZIndex = 1000000
    container.Parent = MainFrame
    container.ScrollingDirection = Enum.ScrollingDirection.Y
    container.ScrollBarThickness = 4
    container.ScrollBarImageColor3 = Color3.fromRGB(120, 140, 255)
    container.CanvasSize = UDim2.new(0, 0, 0, 0)
    container.AutomaticCanvasSize = Enum.AutomaticSize.Y
    container.Visible = false

    local tabBtn = Instance.new("TextButton")
    tabBtn.Size = UDim2.new(1 / math.max(#self.Tabs + 1, 1), -3, 1, -6)
    tabBtn.Position = UDim2.new(#self.Tabs / math.max(#self.Tabs + 1, 1), 0, 0, 3)
    tabBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    tabBtn.BorderSizePixel = 0
    tabBtn.Text = (icon and (icon .. "  ") or "") .. name
    tabBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
    tabBtn.Font = Enum.Font.GothamBold
    tabBtn.TextSize = 14
    tabBtn.AutoButtonColor = false
    tabBtn.ZIndex = 1000001
    tabBtn.Parent = TabPanel

    Rounded(tabBtn, 15)

    tabBtn.MouseButton1Click:Connect(function()
        for _, cont in pairs(self.Containers) do
            cont.Visible = false
        end
        container.Visible = true

        for _, tab in pairs(self.Tabs) do
            tab.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
            tab.TextColor3 = Color3.fromRGB(150, 150, 150)
        end
        TweenService:Create(tabBtn, TweenInfo.new(0.15), {
            BackgroundColor3 = Color3.fromRGB(70, 70, 80)
        }):Play()
        tabBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
    end)

    table.insert(self.Tabs, tabBtn)
    self.Containers[name] = container

    local count = #self.Tabs
    for i, tab in pairs(self.Tabs) do
        tab.Size = UDim2.new(1 / count, -3, 1, -6)
        tab.Position = UDim2.new((i - 1) / count, 0, 0, 3)
    end

    if #self.Tabs == 1 then
        container.Visible = true
        tabBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 80)
        tabBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
    end

    return container
end

function Menu:CreateToggle(container, yPos, labelText, default, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 55)
    frame.Position = UDim2.new(0, 0, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    frame.BorderSizePixel = 0
    frame.ZIndex = 1000001
    frame.Parent = container

    Rounded(frame, 12)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.Position = UDim2.new(0, 15, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(200, 200, 200)
    label.Font = Enum.Font.Gotham
    label.TextSize = 15
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 1000002
    label.Parent = frame

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 55, 0, 28)
    btn.Position = UDim2.new(0.85, 0, 0.5, -14)
    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
    btn.BorderSizePixel = 0
    btn.Text = ""
    btn.AutoButtonColor = false
    btn.ZIndex = 1000002
    btn.Parent = frame

    Rounded(btn, 14)

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 20, 0, 20)
    knob.Position = UDim2.new(0, default and 31 or 4, 0.5, -10)
    knob.BackgroundColor3 = default and Color3.fromRGB(255,255,255) or Color3.fromRGB(150,150,150)
    knob.BorderSizePixel = 0
    knob.ZIndex = 1000003
    knob.Parent = btn

    Rounded(knob, 10)

    if default then
        btn.BackgroundColor3 = Color3.fromRGB(80, 80, 90)
    end

    local isEnabled = default or false

    btn.MouseButton1Click:Connect(function()
        isEnabled = not isEnabled
        if isEnabled then
            TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 90)}):Play()
            TweenService:Create(knob, TweenInfo.new(0.15), {Position = UDim2.new(0, 31, 0.5, -10), BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        else
            TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(60, 60, 70)}):Play()
            TweenService:Create(knob, TweenInfo.new(0.15), {Position = UDim2.new(0, 4, 0.5, -10), BackgroundColor3 = Color3.fromRGB(150, 150, 150)}):Play()
        end
        if callback then callback(isEnabled) end
    end)
end

function Menu:CreateSlider(container, yPos, labelText, min, max, default, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 70)
    frame.Position = UDim2.new(0, 0, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    frame.BorderSizePixel = 0
    frame.ZIndex = 1000001
    frame.Parent = container

    Rounded(frame, 12)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -20, 0, 25)
    label.Position = UDim2.new(0, 15, 0, 5)
    label.BackgroundTransparency = 1
    label.Text = labelText .. ": " .. default
    label.TextColor3 = Color3.fromRGB(200, 200, 200)
    label.Font = Enum.Font.Gotham
    label.TextSize = 14
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 1000002
    label.Parent = frame

    local sliderBtn = Instance.new("TextButton")
    sliderBtn.Size = UDim2.new(1, -30, 0, 25)
    sliderBtn.Position = UDim2.new(0, 15, 0, 35)
    sliderBtn.BackgroundTransparency = 1
    sliderBtn.BorderSizePixel = 0
    sliderBtn.Text = ""
    sliderBtn.AutoButtonColor = false
    sliderBtn.ZIndex = 1000002
    sliderBtn.Parent = frame

    local bg = Instance.new("Frame")
    bg.Size = UDim2.new(1, 0, 0, 5)
    bg.Position = UDim2.new(0, 0, 0.5, -2)
    bg.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
    bg.BorderSizePixel = 0
    bg.ZIndex = 1000003
    bg.Parent = sliderBtn

    Rounded(bg, 3)

    local fill = Instance.new("Frame")
    local percent = (default - min) / (max - min)
    fill.Size = UDim2.new(percent, 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(120, 140, 255)
    fill.BorderSizePixel = 0
    fill.ZIndex = 1000004
    fill.Parent = bg

    Rounded(fill, 3)

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 14, 0, 14)
    knob.Position = UDim2.new(percent, -7, 0.5, -7)
    knob.BackgroundColor3 = Color3.fromRGB(220, 220, 220)
    knob.BorderSizePixel = 0
    knob.ZIndex = 1000005
    knob.Parent = bg

    Rounded(knob, 7)

    local dragging = false

    local function updateValue(inputX)
        local absX = sliderBtn.AbsolutePosition.X
        local width = sliderBtn.AbsoluteSize.X
        local p = math.clamp((inputX - absX) / width, 0, 1)
        local value = math.floor(min + (max - min) * p + 0.5)
        fill.Size = UDim2.new(p, 0, 1, 0)
        knob.Position = UDim2.new(p, -7, 0.5, -7)
        label.Text = labelText .. ": " .. value
        if callback then callback(value) end
    end

    sliderBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            updateValue(input.Position.X)
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)

    RunService.RenderStepped:Connect(function()
        if dragging then
            updateValue(UserInputService:GetMouseLocation().X)
        end
    end)
end

function Menu:CreateColorButton(container, yPos, labelText, colors, defaultColor, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 55)
    frame.Position = UDim2.new(0, 0, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    frame.BorderSizePixel = 0
    frame.ZIndex = 1000001
    frame.Parent = container

    Rounded(frame, 12)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.5, 0, 1, 0)
    label.Position = UDim2.new(0, 15, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(200, 200, 200)
    label.Font = Enum.Font.Gotham
    label.TextSize = 15
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 1000002
    label.Parent = frame

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 80, 0, 30)
    btn.Position = UDim2.new(0.7, 0, 0.5, -15)
    btn.BackgroundColor3 = defaultColor
    btn.BorderSizePixel = 0
    btn.Text = ""
    btn.AutoButtonColor = false
    btn.ZIndex = 1000002
    btn.Parent = frame

    Rounded(btn, 15)

    local colorIndex = 1
    for i, c in pairs(colors) do
        if c == defaultColor then colorIndex = i break end
    end

    btn.MouseButton1Click:Connect(function()
        colorIndex = colorIndex + 1
        if colorIndex > #colors then colorIndex = 1 end
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = colors[colorIndex]}):Play()
        if callback then callback(colors[colorIndex]) end
    end)
end

function Menu:CreateBindButton(container, yPos, labelText, defaultKey, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 0, 55)
    frame.Position = UDim2.new(0, 0, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    frame.BorderSizePixel = 0
    frame.ZIndex = 1000001
    frame.Parent = container

    Rounded(frame, 12)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.5, 0, 1, 0)
    label.Position = UDim2.new(0, 15, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(200, 200, 200)
    label.Font = Enum.Font.Gotham
    label.TextSize = 15
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 1000002
    label.Parent = frame

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 100, 0, 30)
    btn.Position = UDim2.new(0.7, 0, 0.5, -15)
    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
    btn.BorderSizePixel = 0
    btn.Text = defaultKey.Name
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 13
    btn.AutoButtonColor = false
    btn.ZIndex = 1000002
    btn.Parent = frame

    Rounded(btn, 15)

    local waiting = false

    btn.MouseButton1Click:Connect(function()
        waiting = true
        btn.Text = "..."
    end)

    UserInputService.InputBegan:Connect(function(input)
        if waiting and input.UserInputType == Enum.UserInputType.Keyboard then
            waiting = false
            btn.Text = input.KeyCode.Name
            if callback then callback(input.KeyCode) end
        end
    end)
end

function Menu:CreateButton(container, yPos, labelText, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 40)
    btn.Position = UDim2.new(0, 0, 0, yPos)
    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
    btn.BorderSizePixel = 0
    btn.Text = labelText
    btn.TextColor3 = Color3.fromRGB(230, 230, 240)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.AutoButtonColor = false
    btn.ZIndex = 1000001
    btn.Parent = container

    Rounded(btn, 12)

    local btnStroke = Instance.new("UIStroke")
    btnStroke.Color = Color3.fromRGB(120, 140, 255)
    btnStroke.Transparency = 0.5
    btnStroke.Thickness = 1
    btnStroke.Parent = btn

    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 120)}):Play()
    end)

    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(60, 60, 90)}):Play()
    end)

    btn.MouseButton1Click:Connect(function()
        if callback then callback() end
    end)
end

-- ============ ESP SYSTEM ============
local ESP = {
    Enabled = false,
    Highlights = {},
    Connections = {},
    Color = Menu.Defaults.ESPColor,
    CharacterWatchers = {},
}

function ESP:AddChams(player)
    if player == Player then return end
    self:RemoveChams(player)
    local char = player.Character
    if not char or not char.Parent then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum or hum.Health <= 0 then return end

    local hl = Instance.new("Highlight")
    hl.FillColor = self.Color
    hl.OutlineColor = Color3.fromRGB(255, 255, 255)
    hl.FillTransparency = 0.3
    hl.OutlineTransparency = 0
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    hl.Adornee = char
    hl.Parent = workspace
    self.Highlights[player] = hl

    local conn
    conn = hum.Died:Connect(function()
        self:RemoveChams(player)
        if conn then conn:Disconnect() end
    end)
    table.insert(self.Connections, conn)
end

function ESP:RemoveChams(player)
    if self.Highlights[player] then
        pcall(function() self.Highlights[player]:Destroy() end)
        self.Highlights[player] = nil
    end
end

function ESP:WatchCharacter(player)
    if player == Player then return end
    if self.CharacterWatchers[player] then
        self.CharacterWatchers[player]:Disconnect()
        self.CharacterWatchers[player] = nil
    end

    self.CharacterWatchers[player] = player.CharacterAdded:Connect(function(newChar)
        newChar:WaitForChild("Humanoid", 10)
        task.wait(0.5)
        if self.Enabled and newChar.Parent then
            self:AddChams(player)
        end
    end)
end

function ESP:Enable()
    if self.Enabled then return end
    self.Enabled = true

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= Player then
            self:AddChams(p)
            self:WatchCharacter(p)
        end
    end

    table.insert(self.Connections, Players.PlayerAdded:Connect(function(p)
        if self.Enabled and p ~= Player then
            self:AddChams(p)
            self:WatchCharacter(p)
        end
    end))

    table.insert(self.Connections, Players.PlayerRemoving:Connect(function(p)
        self:RemoveChams(p)
        if self.CharacterWatchers[p] then
            self.CharacterWatchers[p]:Disconnect()
            self.CharacterWatchers[p] = nil
        end
    end))
end

function ESP:Disable()
    if not self.Enabled then return end
    self.Enabled = false
    for _, c in pairs(self.Connections) do c:Disconnect() end
    self.Connections = {}
    for p, watcher in pairs(self.CharacterWatchers) do
        if watcher then watcher:Disconnect() end
    end
    self.CharacterWatchers = {}
    for p, hl in pairs(self.Highlights) do
        if hl then pcall(function() hl:Destroy() end) end
    end
    self.Highlights = {}
end

function ESP:SetColor(c)
    self.Color = c
    for _, hl in pairs(self.Highlights) do
        if hl then hl.FillColor = c end
    end
end

-- ============ HITBOX EXPANDER ============
local HitboxExpander = {
    Enabled = false,
    OriginalSizes = {},
    Connections = {},
    SizeMultiplier = Menu.Defaults.HitboxSize,
}

function HitboxExpander:ExpandHead(player)
    if player == Player then return end
    local char = player.Character
    if not char or not char.Parent then return end
    local head = char:FindFirstChild("Head")
    if not head then return end

    if not self.OriginalSizes[player] then
        self.OriginalSizes[player] = head.Size
    end

    head.Size = self.OriginalSizes[player] * Vector3.new(
        self.SizeMultiplier, self.SizeMultiplier, self.SizeMultiplier
    )
    head.CanCollide = false
    head.Massless = true
end

function HitboxExpander:RestoreHead(player)
    local char = player.Character
    if not char then return end
    local head = char:FindFirstChild("Head")
    if not head then return end

    if self.OriginalSizes[player] then
        head.Size = self.OriginalSizes[player]
        self.OriginalSizes[player] = nil
    end
end

function HitboxExpander:SetSize(multiplier)
    self.SizeMultiplier = math.max(multiplier, 0.1)
    if self.Enabled then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= Player then
                self:ExpandHead(p)
            end
        end
    end
end

function HitboxExpander:Enable()
    if self.Enabled then return end
    self.Enabled = true
    print("Hitbox Expander включен")

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= Player then
            self:ExpandHead(p)
            table.insert(self.Connections, p.CharacterAdded:Connect(function()
                task.wait(0.7)
                if self.Enabled then self:ExpandHead(p) end
            end))
        end
    end

    table.insert(self.Connections, Players.PlayerAdded:Connect(function(p)
        if self.Enabled and p ~= Player then
            p.CharacterAdded:Connect(function()
                task.wait(0.7)
                if self.Enabled then self:ExpandHead(p) end
            end)
            if p.Character then self:ExpandHead(p) end
        end
    end))

    table.insert(self.Connections, Players.PlayerRemoving:Connect(function(p)
        self:RestoreHead(p)
    end))
end

function HitboxExpander:Disable()
    if not self.Enabled then return end
    self.Enabled = false
    print("Hitbox Expander выключен")

    for _, c in pairs(self.Connections) do c:Disconnect() end
    self.Connections = {}

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= Player then self:RestoreHead(p) end
    end

    self.OriginalSizes = {}
end

-- ============ AIMBOT SYSTEM ============
local Aimbot = {
    Enabled = false,
    FOV = Menu.Defaults.AimbotFOV,
    FOVCircle = nil,
    Connection = nil,
    Smoothness = Menu.Defaults.AimbotSmooth,
    AimKey = Menu.Defaults.AimbotKey,
    _savedCamType = nil,
    _savedCamSubject = nil,
}

local function IsVisible(target)
    local char = Player.Character
    if not char then return false end
    local head = char:FindFirstChild("Head")
    if not head then return false end
    local targetHead = target:FindFirstChild("Head")
    if not targetHead then return false end

    local params = RaycastParams.new()
    params.FilterDescendantsInstances = {char}
    params.FilterType = Enum.RaycastFilterType.Blacklist
    local dir = (targetHead.Position - head.Position).Unit
    local dist = (targetHead.Position - head.Position).Magnitude
    local result = workspace:Raycast(head.Position, dir * dist, params)

    if result then
        local model = result.Instance:FindFirstAncestorOfClass("Model")
        return model and model == target or false
    end
    return true
end

local function FindTarget()
    local char = Player.Character
    if not char then return nil end
    local cam = workspace.CurrentCamera
    if not cam then return nil end

    local center = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y / 2)
    local fovRadius = (Aimbot.FOV / 2) * (cam.ViewportSize.Y / 1080) * 5
    local targets = {}

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= Player then
            local targetChar = p.Character
            if targetChar then
                local hum = targetChar:FindFirstChild("Humanoid")
                if hum and hum.Health > 0 then
                    local targetHead = targetChar:FindFirstChild("Head")
                    if targetHead and IsVisible(targetChar) then
                        local sp = cam:WorldToScreenPoint(targetHead.Position)
                        if sp.Z > 0 then
                            local distFromCenter = (Vector2.new(sp.X, sp.Y) - center).Magnitude
                            if distFromCenter <= fovRadius then
                                local root = char:FindFirstChild("HumanoidRootPart")
                                local targetRoot = targetChar:FindFirstChild("HumanoidRootPart")
                                if root and targetRoot then
                                    table.insert(targets, {
                                        Head = targetHead,
                                        Distance = (root.Position - targetRoot.Position).Magnitude,
                                    })
                                end
                            end
                        end
                    end
                end
            end
        end
    end

    table.sort(targets, function(a, b) return a.Distance < b.Distance end)
    return #targets > 0 and targets[1].Head or nil
end

function Aimbot:CreateFOVCircle()
    if self.FOVCircle then self.FOVCircle:Destroy() end

    local gui = Instance.new("ScreenGui")
    gui.Name = "FOVCircle"
    gui.Parent = Player:WaitForChild("PlayerGui")
    gui.ResetOnSpawn = false
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Global
    gui.DisplayOrder = 999998
    gui.IgnoreGuiInset = true

    local cam = workspace.CurrentCamera
    local radius = (self.FOV / 2) * (cam.ViewportSize.Y / 1080) * 5

    local circle = Instance.new("Frame")
    circle.AnchorPoint = Vector2.new(0.5, 0.5)
    circle.Size = UDim2.new(0, radius * 2, 0, radius * 2)
    circle.Position = UDim2.new(0.5, 0, 0.5, 0)
    circle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    circle.BackgroundTransparency = 0.85
    circle.BorderSizePixel = 0
    circle.ZIndex = 999999
    circle.Parent = gui

    Rounded(circle, radius)

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(120, 140, 255)
    stroke.Thickness = 1
    stroke.Transparency = 0.3
    stroke.Parent = circle

    self.FOVCircle = gui
end

function Aimbot:Enable()
    if self.Enabled then return end
    self.Enabled = true
    print("Aimbot включен")

    local cam = workspace.CurrentCamera
    if cam then
        self._savedCamType = cam.CameraType
        self._savedCamSubject = cam.CameraSubject
    end

    self:CreateFOVCircle()

    self.Connection = RunService.RenderStepped:Connect(function()
        if not UserInputService:IsKeyDown(self.AimKey) then return end
        local target = FindTarget()
        if target then
            local cam = workspace.CurrentCamera
            if cam then
                local lookAt = CFrame.lookAt(cam.CFrame.Position, target.Position)
                cam.CFrame = cam.CFrame:Lerp(lookAt, self.Smoothness / 100)
            end
        end
    end)
end

function Aimbot:Disable()
    if not self.Enabled then return end
    self.Enabled = false
    print("Aimbot выключен")

    if self.Connection then self.Connection:Disconnect() self.Connection = nil end
    if self.FOVCircle then self.FOVCircle:Destroy() self.FOVCircle = nil end

    local cam = workspace.CurrentCamera
    if cam then
        if self._savedCamType then cam.CameraType = self._savedCamType end
        if self._savedCamSubject then cam.CameraSubject = self._savedCamSubject end
    end
end

-- ============ РЕГИСТРАЦИЯ ВКЛАДОК ============

local ESPTab = Menu:CreateTab("ESP", "👁")
Menu:CreateToggle(ESPTab, 5, "Включить ESP", false, function(enabled)
    if enabled then ESP:Enable() else ESP:Disable() end
end)

local colors = {
    Color3.fromRGB(255, 0, 0),
    Color3.fromRGB(0, 0, 255),
    Color3.fromRGB(0, 255, 0),
    Color3.fromRGB(255, 255, 0),
    Color3.fromRGB(255, 0, 255),
    Color3.fromRGB(0, 255, 255),
}

Menu:CreateColorButton(ESPTab, 65, "Цвет ESP", colors, colors[1], function(color)
    ESP:SetColor(color)
end)

local AimbotTab = Menu:CreateTab("AIMBOT", "")
Menu:CreateToggle(AimbotTab, 5, "Включить Aimbot", false, function(enabled)
    if enabled then Aimbot:Enable() else Aimbot:Disable() end
end)

Menu:CreateBindButton(AimbotTab, 65, "Клавиша аима", Aimbot.AimKey, function(key)
    Aimbot.AimKey = key
    print("Клавиша аима: " .. key.Name)
end)

Menu:CreateSlider(AimbotTab, 125, "FOV", 20, 200, 60, function(value)
    Aimbot.FOV = value
    if Aimbot.Enabled then Aimbot:CreateFOVCircle() end
end)

Menu:CreateSlider(AimbotTab, 200, "Скорость", 1, 100, 35, function(value)
    Aimbot.Smoothness = value
end)

local HitboxTab = Menu:CreateTab("HITBOX", "")
Menu:CreateToggle(HitboxTab, 5, "Включить Hitbox", false, function(enabled)
    if enabled then
        HitboxExpander:Enable()
    else
        HitboxExpander:Disable()
    end
end)

Menu:CreateSlider(HitboxTab, 65, "Размер головы", 1, 5, 2.5, function(value)
    HitboxExpander:SetSize(value)
end)

local SettingsTab = Menu:CreateTab("SETTINGS", "⚙")
Menu:CreateButton(SettingsTab, 5, "  Reset All Settings", function()
    -- Сброс Aimbot
    Aimbot.FOV = Menu.Defaults.AimbotFOV
    Aimbot.Smoothness = Menu.Defaults.AimbotSmooth
    Aimbot.AimKey = Menu.Defaults.AimbotKey
    if Aimbot.Enabled then Aimbot:CreateFOVCircle() end
    -- Сброс Hitbox
    HitboxExpander:SetSize(Menu.Defaults.HitboxSize)
    -- Сброс ESP цвета
    ESP:SetColor(Menu.Defaults.ESPColor)
    print(" Настройки сброшены к дефолту")
end)

Menu:CreateButton(SettingsTab, 55, "  Panic (вырубить всё)", function()
    pcall(function() ESP:Disable() end)
    pcall(function() Aimbot:Disable() end)
    pcall(function() HitboxExpander:Disable() end)
    print(" Panic: всё выключено")
end)

-- ============ ПЛАВНОЕ ОТКРЫТИЕ/ЗАКРЫТИЕ ============

local isOpen = false

local function OpenMenu()
    if isOpen then return end
    isOpen = true
    MainFrame.Visible = true
    MainFrame.Size = UDim2.new(0, 400, 0, 0)
    MainFrame.Position = UDim2.new(0.5, -200, 0.5, -250)
    TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quint), {
        Size = UDim2.new(0, 400, 0, 500)
    }):Play()
end

local function CloseMenu()
    if not isOpen then return end
    isOpen = false
    local tween = TweenService:Create(MainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quint), {
        Size = UDim2.new(0, 400, 0, 0)
    })
    tween:Play()
    tween.Completed:Connect(function()
        MainFrame.Visible = false
    end)
end

-- ============ ОБРАБОТЧИКИ КНОПОК ============

KillButton.MouseButton1Click:Connect(function()
    pcall(function() ESP:Disable() end)
    pcall(function() Aimbot:Disable() end)
    pcall(function() HitboxExpander:Disable() end)
    if ScreenGui then ScreenGui:Destroy() end
    print("Меню уничтожено")
end)

KillButton.MouseEnter:Connect(function()
    TweenService:Create(KillButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(180, 60, 60)}):Play()
end)

KillButton.MouseLeave:Connect(function()
    TweenService:Create(KillButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(120, 40, 40)}):Play()
end)

CloseButton.MouseButton1Click:Connect(function()
    CloseMenu()
end)

CloseButton.MouseEnter:Connect(function()
    TweenService:Create(CloseButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(150, 60, 60)}):Play()
end)

CloseButton.MouseLeave:Connect(function()
    TweenService:Create(CloseButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(50, 50, 55)}):Play()
end)

-- ============ ПЕРЕТАСКИВАНИЕ ОКНА ============

local dragging = false
local dragOffset = Vector2.new(0, 0)
local lastPosition = nil

Header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        lastPosition = input.Position
        dragOffset = Vector2.new(
            input.Position.X - MainFrame.AbsolutePosition.X,
            input.Position.Y - MainFrame.AbsolutePosition.Y
        )
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and lastPosition and input.UserInputType == Enum.UserInputType.MouseMovement then
        lastPosition = input.Position
    end
end)

RunService.RenderStepped:Connect(function()
    if dragging and lastPosition then
        MainFrame.Position = UDim2.new(0, lastPosition.X - dragOffset.X, 0, lastPosition.Y - dragOffset.Y)
    end
end)

-- ============ ХОТКЕИ ============

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end

    -- Insert → открыть/закрыть
    if input.KeyCode == Enum.KeyCode.Insert then
        if isOpen then CloseMenu() else OpenMenu() end
    end

    -- RightShift → паника (вырубить всё)
    if input.KeyCode == Enum.KeyCode.RightShift then
        pcall(function() ESP:Disable() end)
        pcall(function() Aimbot:Disable() end)
        pcall(function() HitboxExpander:Disable() end)
        print(" PANIC (RightShift): всё выключено")
    end
end)

print(" NEXUS HUB загружен!")
print(" Insert — открыть/закрыть меню")
print(" RightShift — паника (вырубить все читы)")
